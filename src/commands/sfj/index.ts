import { Command } from '@oclif/command'

export default class Functions extends Command {
  static description = 'StoreFramework serverless functions'

  static examples = [`$ vtex-test sfj start`]

  async run() {
    // do nothing.
  }
}
